import 'package:flutter/material.dart';

class Custom_Camera extends StatefulWidget {
  const Custom_Camera({super.key});

  @override
  State<Custom_Camera> createState() => _Custom_CameraState();
}

class _Custom_CameraState extends State<Custom_Camera> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
}